language: php

php:
  - 7.4.6
  
install:
  - composer install
  
addons:
  sonarcloud:
    organization: "groundservices"
    token:
      secure: $SONAR_TOKEN

before_script:
  - cp .env.travis .env    
  
script:
  - php artisan key:generate
  - sonar-scanner
